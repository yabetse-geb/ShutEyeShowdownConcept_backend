Now users can update their accounts by modifying their email and password (with new actions changePassword and changeEmail respectively). Users can also deactivate/delete their account via the deactivateAccount action. Additionally, added queries isRegistered (identifies whether or not a username is registered), and getEmail which returns the email associated with a username. 